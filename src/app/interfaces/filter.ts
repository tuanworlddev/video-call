export interface Filter {
  filterId: number
  name: number,
  blur: number,
  brightness: number,
  contrast: number,
  grayscale: number,
  hueRotate: number,
  invert: number,
  opacity: number,
  saturate: number,
  sepia: number,
}
